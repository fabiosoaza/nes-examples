version: '3'

services:
      examples:
            image: "fabiosoaza/snes-dev-tools:latest" 
            command: /bin/bash -c "cd /tmp/nes-vertical-scrolling && ./build.sh"
            volumes: 
                  - ./:/tmp/nes-vertical-scrolling
  
