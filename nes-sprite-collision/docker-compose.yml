version: '3'

services:
      examples:
            image: "fabiosoaza/snes-dev-tools:latest" 
            command: /bin/bash -c "cd /tmp/nes-sprite-collision && ./build.sh"
            volumes: 
                  - ./:/tmp/nes-sprite-collision
  
